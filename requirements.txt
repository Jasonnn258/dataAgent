# core
pydantic>=2.0
# parsing
# NOTE: tree-sitter 0.26.0 + grammar wheels 0.23.x = ABI mismatch -> segfault in GC.
# Pin 0.25.x until grammars publish 0.26-compatible wheels.
# Verified working set (2026-09-20): ts 0.25.2 + ts-typescript 0.23.2 + ts-javascript 0.25.0
tree-sitter>=0.25,<0.26
tree-sitter-typescript>=0.23,<0.24
tree-sitter-javascript>=0.23,<0.26
networkx>=3.0
# optional: context graph (mode=semantica)
# Full install pulls the scientific stack (numpy/sklearn/umap-learn...).
# The parts we use (semantica.kg + semantica.provenance) are pure python;
# --no-deps works if numpy/networkx are already present.
#   pip install semantica==0.6.8              # full
#   pip install --no-deps semantica==0.6.8    # minimal
# semantica==0.6.8
# optional: LLM reasoning (OpenAI-compatible endpoint); framework runs without it
# openai>=1.40
